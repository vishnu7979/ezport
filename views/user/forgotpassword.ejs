 
    <%- include("../partials/header.ejs") -%>

    <div class="container-fluid p-0 justify-content-center align-items-center signUp min-vh-100"
        style="margin-top: 9%;">
        <div class="container d-flex justify-content-center align-items-center">
            <div class="container col-lg-4 d-flex flex-column justify-content-center align-items-center rounded-5 p-5 bg-white CardOTP" style="box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;">
                <div class="d-flex align-items-center justify-content-center">
                    <img src="/assets/images/tck.png" alt="" class="img-fluid imgOTP">
                </div>
                <div class="mt-2 d-flex flex-column align-items-center">
                    <span class="h4">Enter Your Registered Email</span>
                    <div class="text-danger fw-bold" id="notification"><%=locals.msg ? msg : " "%></div>
                </div>
                <form id="formSubmition" action="/forgotpasswordpost" method="post" onsubmit="return fullyChecking()"
                    style="width:100% " class="mt-3">
                    <div class="credntials d-flex flex-column align-items-center justify-content-center gap-2"
                        style="width: 100%;">
                        <input class="form-control signInput" type="email" placeholder="Email" name="email" id="email"
                            onchange="numberChecking()" required>
                        <span id="Number" class="text text-danger"></span>
                        <button class="btn btn-primary" style="font-size: 13px;">
                            Send OTP
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include("../partials/footer.ejs") -%>
    <script src="/javascripts/validation.js"></script>
</body>

</html>
