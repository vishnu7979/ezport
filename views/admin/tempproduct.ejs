<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <style>
        
        body {
            background-color: #fbfbfb;
        }

        .product-container {
            margin-top: 58px;
            margin-left: 240px;
        }

        .product-card {
    border: 1px solid #ccc; /* Add border to the card */
    background-color: #f8f9fa; /* Light gray background color */
    box-shadow: 0 4px 6px -1px rgba(0,0,0,.1), 0 2px 4px -1px rgba(0,0,0,.06); /* Add shadow */
    border-radius: 8px; /* Optional: Rounded corners */
}

.product-card:hover {
    transform: translateY(-5px);
}

        .product-images {
            position: relative;
        }

        .main-image img {
            width: 200px;
            height: 200px;
        }

        .additional-images img {
    width: 40px;
    height: 40px;
    cursor: pointer;
    margin-left: 5px;
    padding-left: 2px;
    border-radius: 10%;
    padding-top: 10px;
}

        .card {
            border: none;
            width: 100%;  
            height: auto;
        }
        .sidebar {
            position: fixed;
            top: 0;
            bottom: 0;
            left: 0;
            padding: 58px 0 0;
            box-shadow: 0 2px 5px 0 rgba(0, 0, 0, 0.05), 0 2px 10px 0 rgba(0, 0, 0, 0.05);
            width: 240px;
            z-index: 600;
            border-right: 3px solid #6c757d;  
            background-color: #343a40;  /
        }

        @media (max-width: 991.98px) {
            .sidebar {
                width: 100%;
            }
        }

        .sidebar .active {
            border-radius: 5px;
            box-shadow: 0 2px 5px 0 rgba(0, 0, 0, 0.16), 0 2px 10px 0 rgba(0, 0, 0, 0.12);
        }

        .list-group-item {
            border: none;
            padding: 0;  
            font-size: 1rem;
        }

        .list-group-item:hover {
            background-color: rgba(0, 0, 0, 0.05);
        }

        .card-link {
            padding: 8px 16px;
            display: block;
            width: 100%;
            color: #000;
            text-decoration: none;
            border : 1px solid #000000;  
        }

        .card-link:hover {
            background-color: #f1f1f1;
        }
        .card-title {
    font-size: 1rem;  
}

.card-text {
    font-size: 0.9rem;  
}

.card-body {
    padding: 1rem;  
}

.btn {
    font-size: 0.8rem;  
}

 
 
        a {
            text-decoration: none;
        }

        .sidebar-sticky {
            position: relative;
            top: 0;
            height: calc(100vh - 48px);
            padding-top: 0.5rem;
            overflow-x: hidden;
            overflow-y: auto;
        }

        /* Navbar */
        #main-navbar {
            box-shadow: 0 4px 6px -1px rgba(0,0,0,.1), 0 2px 4px -1px rgba(0,0,0,.06);
            border-bottom: 3px solid #afc1d0;  
            background-color: #d1c1c0 !important;  
        }

        .navbar-brand {
            font-weight: bold;
        }

        .navbar-search {
            min-width: 225px;
        }

        .navbar-search input {
            border-radius: 25px;
            border: 1px solid #ced4da;
            padding: 8px 16px;
        }

        .list-group-item a {
            background-color: rgba(0, 0, 0, 0.05);
            display: block;
            padding: 8px 16px;
            text-decoration: none;
            color: inherit;  
        }
        
    </style>
</head>
<body>
    <header>
        
<nav id="sidebarMenu" class="collapse d-lg-block sidebar collapse bg-white">
    <div class="position-sticky">
        <div class="list-group list-group-flush mx-3 mt-4">
           
            <a class="list-group-item list-group-item-action py-2 ripple" aria-current="true"
                data-mdb-toggle="collapse" href="/admin/dashboard" aria-expanded="true"
                aria-controls="collapseExample1">
                <i class="fas fa-tachometer-alt fa-fw me-3"></i><span>Admin Dashboard</span>
            </a>
         
            <ul id="collapseExample1" class="collapse show list-group list-group-flush">
                <li class="list-group-item py-1">
                    <div class="card border-dark mb-3">
                        <a href="/admin/userDetails" class="card-link">User Details</a>
                    </div>
                </li>
                <li class="list-group-item py-1">
                    <div class="card border-dark mb-3">
                        <a href="/admin/addproduct" class="card-link">Add Product</a>
                    </div>
                </li>
                <li class="list-group-item py-1">
                    <div class="card border-dark mb-3">
                        <a href="/admin/showproducts" class="card-link">Show Products</a>
                    </div>
                </li>
                <li class="list-group-item py-1">
                    <div class="card border-dark mb-3">
                        <a href="/admin/categoryManagement" class="card-link">Category Management</a>
                    </div>
                </li>
                <li class="list-group-item py-1">
                    <div class="card border-dark mb-3">
                        <a href="/admin/OrderManagement" class="card-link">Order Management</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

        
<nav id="main-navbar" class="navbar navbar-expand-lg navbar-light bg-white fixed-top">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/admin/dashboard">Admin Dashboard</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <form class="d-none d-lg-flex input-group w-auto my-auto ms-auto navbar-search">
                <input autocomplete="off" type="search" class="form-control rounded"
                    placeholder='Search Here' />
                <span class="input-group-text border-0"><i class="fas fa-search"></i></span>
            </form>
        </div>
    </div>
</nav>

        
    </header>

    <div class="container product-container">
        <h1>List of Products</h1>
        <form action="/admin/showproducts" method="GET" class="mb-3">
            <div class="input-group">
                <input type="text" name="searchQuery" class="form-control" placeholder="Search products">
                <button type="submit" class="btn btn-primary">Search</button>
            </div>
        </form>

        <div class="row row-cols-1 row-cols-md-3 g-4 justify-content-evenly">
            <% products.forEach(product => { %>
                <div class="col-md-4  ">
                    <div class="card h-100 product-card shadow ">
                             <div class="product-images">
                                <div class="main-image">
                                    <img src="/uploads/<%= product.image %>" alt="<%= product.name %>">
                                </div>
                                <div class="additional-images">
                                    <% for (let i = 0; i < product.additionalImages.length; i++) { %>
                                        <img src="/uploads/<%= product.additionalImages[i] %>" alt="<%= product.name %>" class="additional-image">
                                    <% } %>
                                </div>
                            </div>
                        <div class="card-body">
                            <h5 class="card-title"><%= product.name %></h5>
                            <p class="card-text">Description: <%= product.description %></p>
                            <p class="card-text">Price: <%= product.price %></p>
                            <p class="card-text">Category: <%= product.category %></p>
                             
                                <div class="btn-group">
                            <a href="/admin/editproduct/<%= product._id %>" class="btn btn-primary btn-sm">Edit</a>
                            <% if (product.isDeleted) { %>
                                <form action="/admin/toggleproductavailability/<%= product._id %>" method="post">
                                    <button type="submit" class="btn btn-danger btn-sm">Undelete</button>
                                </form>
                            <% } else { %>
                                <form action="/admin/toggleproductavailability/<%= product._id %>" method="post">
                                    <button type="submit" id="hi" class="btn btn-danger btn-sm">Delete</button>
                                </form>
                            <% } %>
                        </div>

                            
                    </div>
                </div>
            </div>
        <% }); %>
    </div>


 
<div class="pagination mt-4">
    <% if (page > 1) { %>
        <button><a href="/admin/showproducts?page=<%= page - 1 %>&searchQuery=<%= searchQuery %>&limit=<%= limit %>">Previous</a></button> 
    <% } %>

    <% if (products.length === limit && products.length > 0) { %>
        <button><a href="/admin/showproducts?page=<%= page + 1 %>&searchQuery=<%= searchQuery %>&limit=<%= limit %>">Next</a></button> 
    <% } %>
</div>
</div>          

   


    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const productImages = document.querySelectorAll('.product-images');
    
            productImages.forEach(imagesContainer => {
                const additionalImages = imagesContainer.querySelectorAll('.additional-images img');
                const mainImage = imagesContainer.querySelector('.main-image img');
    
                additionalImages.forEach(image => {
                    image.addEventListener('click', function() {
                        const clickedImageSrc = this.src;
                        mainImage.src = clickedImageSrc;
                    });
                });
            });
        });
    </script>
    
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js"></script>
    


</body>
</html>





